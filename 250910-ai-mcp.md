- [MCP란 무엇인가?](#mcp란-무엇인가)
  - [정의](#정의)
  - [특징](#특징)
- [활용 예시](#활용-예시)
  - [예시 1: 웹사이트 뉴스 가져오기](#예시-1-웹사이트-뉴스-가져오기)
  - [예시 2: Notion 페이지 생성 및 검색](#예시-2-notion-페이지-생성-및-검색)
  - [예시 3: 로컬 파일 시스템 제어](#예시-3-로컬-파일-시스템-제어)

---

## MCP란 무엇인가?

### 정의
- **MCP (Model Context Protocol)** 은 **AI 모델이 외부 앱/서비스와 통신하기 위한 표준화된 프로토콜**.
- OpenAI가 제안한 규격으로, 모델이 다양한 툴(검색, 캘린더, DB, API 등)에 접근할 수 있도록 **확장 가능한 인터페이스**를 제공.

### 특징
- 모델이 직접 "API 스펙"을 이해하지 않고, MCP를 통해 안전하게 요청·응답.
- 다양한 도구/서비스와의 연결성을 높이고, **AI 앱 생태계 확장**에 활용됨.
- 보안/권한 제어 가능.

---

## 활용 예시

### 예시 1: 웹사이트 뉴스 가져오기

- 간단한 MCP 서버를 직접 구축하여 AI 모델이 특정 웹사이트의 콘텐츠를 가져오게 하는 예시.
- `@mcp.tool()` 데코레이터를 사용하여 `get_tech_news` 함수를 AI 모델이 호출할 수 있는 '도구'로 등록.

```python
# 필요 라이브러리: httpx, beautifulsoup4, mcp_server
import httpx
from bs4 import BeautifulSoup
from mcp_server import FastMCP, mcp

# MCP 서버 초기화
mcp = FastMCP("tech_news_fetcher")

NEWS_SITES = {
    "arstechnica": "https://arstechnica.com"
}

@mcp.tool()
async def get_tech_news(source: str):
    """
    특정 기술 뉴스 사이트에서 최신 뉴스를 가져와 요약합니다.
    Args:
        source: 뉴스 소스 이름 (예: "arstechnica").
    Returns:
        최신 뉴스 요약.
    """
    if source not in NEWS_SITES:
        raise ValueError(f"지원하지 않는 소스입니다: {source}")

    async with httpx.AsyncClient() as client:
        try:
            response = await client.get(NEWS_SITES[source], timeout=30.0)
            soup = BeautifulSoup(response.text, "html.parser")
            # 첫 5개 문단만 요약
            paragraphs = soup.find_all("p")
            text = " ".join([p.get_text() for p in paragraphs[:5]])
            return text
        except httpx.TimeoutException:
            return "요청 시간 초과"

if __name__ == "__main__":
    # MCP 클라이언트가 접속할 수 있도록 서버 실행
    mcp.run(transport="stdio")
```

### 예시 2: Notion 페이지 생성 및 검색

- Notion과 같이 자체적으로 MCP 서버를 제공하는 서비스와 연동하는 예시.
- AI 툴(예: Claude Desktop)의 설정 파일에 Notion 서버 정보를 추가하여 연결.

**1. Notion 공개 MCP 서버 접속 설정**

- AI 툴의 설정 파일(JSON)에 Notion 서버 URL을 추가.

```json
{
  "mcpServers": {
    "Notion": {
      "url": "https://mcp.notion.com/mcp"
    }
  }
}
```

**2. 로컬 서버를 브릿지로 사용 (고급)**

- Notion API 키를 사용하여 로컬에서 MCP 서버를 실행하고, AI 툴이 이 로컬 서버를 바라보게 하는 방식.

```bash
# 로컬 서버 클론 및 설정
git clone https://github.com/ccabanillas/notion-mcp.git
cd notion-mcp
pip install -e .

# .env 파일에 Notion API 키 저장
echo "NOTION_API_KEY=YOUR_NOTION_API_KEY" > .env
```

- AI 툴 설정 파일에 로컬 서버 실행 명령 추가.

```json
{
  "servers": {
    "notion-mcp": {
      "command": "/path/to/notion-mcp/.venv/bin/python",
      "args": ["-m", "notion_mcp"],
      "cwd": "/path/to/notion-mcp"
    }
  }
}
```

### 예시 3: 로컬 파일 시스템 제어

- AI 모델이 로컬 컴퓨터의 파일을 읽거나 쓸 수 있도록 MCP 서버를 설정하는 예시.
- 보안을 위해 특정 디렉토리만 접근하도록 제한 가능.

**1. 파일 읽기/쓰기 도구 구현**

- `mcp_server_filesystem` 같은 오픈소스를 활용하여 파일 제어 도구를 구현.
- `list_directory`, `read_file`, `save_file` 등의 함수를 도구로 등록.

**2. 서버 실행**

- 특정 프로젝트 디렉토리를 지정하여 MCP 서버를 실행.

```bash
mcp-server-filesystem --project-dir /path/to/your/project
```

- AI 모델은 이 서버를 통해 `/path/to/your/project` 내의 파일을 읽고 쓸 수 있게 됨.
- 예를 들어, 모델에게 "프로젝트 내 모든 `.py` 파일 목록을 보여줘" 또는 "새로운 기능에 대한 아이디어를 `ideas.md` 파일에 저장해줘" 같은 작업을 시킬 수 있음.

---