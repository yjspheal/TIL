- [비트 연산 기본](#비트-연산-기본)
- [비트마스크 활용](#비트마스크-활용)
- [비트 카운트(bit\_count)](#비트-카운트bit_count)
  - [대체 방법](#대체-방법)
- [PyPy와 CPython의 차이](#pypy와-cpython의-차이)
- [배운 점/정리](#배운-점정리)


---

## 비트 연산 기본

- **비트 연산자**
  | 연산자 | 의미 | 예시 |
  |-------|------|------|
  | `&`   | AND (둘 다 1일 때만 1) | `6 & 3 = 2` |
  | `\|`   | OR (둘 중 하나라도 1이면 1) | `6 \| 3 = 7` |
  | `^`   | XOR (둘이 다르면 1) | `6 ^ 3 = 5` |
  | `~`   | NOT (비트 반전, 1의 보수) | `~6 = -7` |
  | `<<`  | 왼쪽 시프트 (2배씩 커짐) | `3 << 1 = 6` |
  | `>>`  | 오른쪽 시프트 (2로 나눔) | `6 >> 1 = 3` |

- 특징
  - 정수(int)는 내부적으로 이진수로 저장 → 비트 연산 가능
  - `<<`, `>>`는 곱셈/나눗셈보다 빠른 경우가 많아 **최적화에 활용**

---

## 비트마스크 활용

- 특정 비트 켜기/끄기/토글하기
  ~~~python
  x = 0b1010   # 10
  mask = 1 << 2  # 3번째 비트만 1

  # 켜기
  x_on = x | mask

  # 끄기
  x_off = x & ~mask

  # 토글
  x_toggle = x ^ mask
  ~~~

- 부분집합, 방문 체크, 상태 압축 등에 활용 (ex. 알고리즘 문제)

---

## 비트 카운트(bit_count)

- Python 3.8 이후 CPython에서 제공:  
  정수의 이진 표현에서 `1`의 개수를 세는 메서드
  ~~~python
  (13).bit_count()   # 3 (1101 → 1이 세 개)
  ~~~

- PyPy에서는 **`bit_count()` 메서드를 지원하지 않음** → 다른 방법 필요

### 대체 방법
1. `bin(n).count("1")`
   ```python
   bin(13).count("1")  # 3
Brian Kernighan’s Algorithm

~~~python

def count_bits(x):
    cnt = 0
    while x:
        x &= x - 1
        cnt += 1
    return cnt
~~~

## PyPy와 CPython의 차이
- CPython: 파이썬 표준 구현체, .bit_count() 사용 가능

- PyPy: JIT 기반 구현체, 일부 내장 메서드 미지원 (특히 최신 문법/메서드)

- 따라서 PyPy 환경에서 비트 연산 최적화를 하려면:

   - bin().count("1") 사용

  - 직접 함수 구현

  - 또는 numpy/bitarray 같은 외부 라이브러리 활용

## 배운 점/정리
- 비트 연산은 빠른 연산, 상태 압축, 최적화에 유용

- bit_count()는 직관적이고 빠르지만, PyPy에서는 쓸 수 없다
