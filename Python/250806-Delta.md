
## 델타좌표를 응용한 탐색

### 정의 및 개념

- **델타좌표(Delta Coordinates)**란, 2차원 격자(행렬/맵)에서 상하좌우(혹은 8방향) 인접 칸을 **효율적으로 탐색**하기 위한 좌표 변화값(증분값, delta)의 집합을 의미함.
- `dr`, `dc` 혹은 `dx`, `dy`로 표기하여 **반복문 1개로 모든 방향의 칸을 검사**할 수 있게 해줌.

### 동작 원리

- 상하좌우 4방향:
    - 행(row), 열(col) 변화량을 튜플/리스트로 저장
    - 예) dr = [-1, 1, 0, 0], dc = [0, 0, -1, 1]
- 8방향(대각선 포함):  
    - dr = [-1, -1, -1, 0, 0, 1, 1, 1]
    - dc = [-1, 0, 1, -1, 1, -1, 0, 1]

### 장점

- 코드의 **반복/중복 감소**
- 범위/경계 검사 쉽게 구현 가능
- BFS/DFS 등 각종 맵 탐색 문제에서 필수 패턴

### 활용 사례

- 미로 찾기, 섬 개수 세기, 파이프 탐색, 색칠 문제 등 **격자 기반 알고리즘**에서 탐색 로직
- BFS, DFS, 완전탐색 구현 시 반복문 1~2개로 인접 칸을 쉽게 체크

---

## Python 코드 예시

```python
# 4방향 델타 (상, 하, 좌, 우)
dr = [-1, 1, 0, 0]
dc = [0, 0, -1, 1]

N, M = 5, 5
grid = [[0]*M for _ in range(N)]  # 5x5 예시

r, c = 2, 2  # 기준 위치
for d in range(4):
    nr, nc = r + dr[d], c + dc[d]
    if 0 <= nr < N and 0 <= nc < M:
        print(f"이동 위치: ({nr}, {nc}), 값: {grid[nr][nc]}")

# 8방향 탐색 예시
dr8 = [-1, -1, -1, 0, 0, 1, 1, 1]
dc8 = [-1, 0, 1, -1, 1, -1, 0, 1]

for d in range(8):
    nr, nc = r + dr8[d], c + dc8[d]
    if 0 <= nr < N and 0 <= nc < M:
        print(f"8방향 이동: ({nr}, {nc})")
