# TIL - 2025-07-30

- [슈도 코드란 무엇인가](#슈도-코드란-무엇인가)
- [버블 정렬](#버블-정렬)
- [카운팅 정렬](#카운팅-정렬)

---

## 슈도 코드란 무엇인가

### 정의
- 실제 프로그래밍 언어의 문법 대신, 알고리즘의 절차를 사람이 읽기 쉬운 방식으로 기술하는 "가짜 코드"
- 논리 흐름을 명확히 전달하고자 할 때 사용합니다.

### 특징
- 자연어와 코드 스타일이 섞인 형태로 자유롭게 작성
- 프로그래밍 언어나 플랫폼에 구애받지 않음
- 들여쓰기, 조건문, 반복문 등 실제 코드와 유사한 구조를 가짐

### 장점
- 알고리즘의 **핵심 논리**를 명확히 전달할 수 있음
- 협업이나 알고리즘 설계 단계에서 이해·의사소통이 쉬움
- 문법 오류 걱정 없이 논리 구조에 집중 가능

### 예시
```
1. 리스트의 각 값을 차례로 확인
2. 값이 짝수면 결과 리스트에 추가
3. 결과 리스트 반환
```

또는 코드 스타일로
```python
for element in 리스트:
    if element가 짝수면:
        결과리스트에 추가
return 결과리스트
```

---

## 버블 정렬

### 개념
- 인접한 두 값을 비교해 순서가 맞지 않으면 **교환**하며, 가장 큰 값(또는 작은 값)이 반복적으로 끝으로 이동하는 방식의 정렬
- 매 라운드마다 가장 큰 값이 맨 뒤로 밀려남

### 동작 원리
1. 리스트 처음부터 끝까지 인접한 값끼리 비교
2. 순서가 틀린 경우 교환
3. 한 바퀴 순회 후 마지막 값은 정렬됨
4. 정렬 안된 부분 반복해서 위 과정 수행
5. 교환이 없으면 정렬 완료

### 시간복잡도

| 경우  | 시간복잡도        |
|------|------------------|
| 최선 | O(n)             |
| 평균 | O(n^2)           |
| 최악 | O(n^2)           |

*최선은 "이미 정렬된 경우, 개선된 버블정렬에서 교환 발생 안 하면 조기종료"*

### 장단점

| 장점                                  | 단점                                 |
|--------------------------------------|--------------------------------------|
| 구현이 매우 쉽다                      | 비효율적(O(n^2)), 대규모 데이터에 부적합 |
| 추가 메모리 필요 없음(제자리 정렬)      | 더 나은 정렬(삽입/선택/퀵 등)이 많음    |
| 거의 정렬된 데이터는 빨리 처리함         |                                      |

### 활용 사례
- 주로 **알고리즘 입문/교육용**
- 데이터 크기 작거나, 거의 정렬된 리스트 정렬

### Python 코드 예시

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        swapped = False
        for j in range(n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped:
            break
    return arr
```

# 예시
data = [5, 2, 9, 1, 5, 6]
print(bubble_sort(data))  # [1, 2, 5, 5, 6, 9]

---

## 카운팅 정렬

### 개념
- 값 자체를 비교하지 않고, **등장 횟수(카운트)**를 미리 세어 정렬하는 방식
- 데이터가 모두 정수(양의 정수/0 이상)이어야 사용 가능

### 동작 원리
1. 데이터 중 **최댓값** 구함
2. [0, 최댓값] 크기의 카운트 배열(counts) 생성
3. 각 값이 등장한 횟수를 카운트 배열에 저장
4. counts를 누적하거나, 카운트만큼 결과 리스트에 값 복원

### 시간복잡도

|      | 시간복잡도       |
|------|-----------------|
| 전체 | O(n + k)        |
|      | (n: 데이터 수, k: 값의 범위) |

### 장단점 및 제한사항

| 장점                                   | 단점/제한사항                                    |
|----------------------------------------|------------------------------------------------|
| 매우 빠름 (O(n+k)), 비교 연산 없음        | 값이 정수(음수/실수 불가), 값 범위(최댓값)가 커지면 비효율 |
| 추가 메모리(카운트 배열) 필요             | 값의 범위 제한(최댓값 기준)                      |
| **안정 정렬** 구현 가능                  |                                                 |

### 활용 사례
- **시험 점수 집계, 투표 집계** 등 값의 범위가 좁고 양의 정수인 경우
- 대량의 로그에서 특정 값 빈도수 세기 등

### Python 코드 예시
```python
def counting_sort(arr):
    if not arr:
        return arr
    max_val = max(arr)
    counts = [0] * (max_val + 1)
    for num in arr:
        counts[num] += 1
    result = []
    for num, cnt in enumerate(counts):
        result.extend([num] * cnt)
    return result
```

# 예시
```python
data = [0, 4, 1, 3, 1, 2, 4, 1]
print(counting_sort(data))  # [0, 1, 1, 1, 2, 3, 4, 4]
```
---
