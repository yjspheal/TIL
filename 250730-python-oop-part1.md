- [절차 지향과 객체 지향](#절차-지향과-객체-지향)
  - [절차 지향 프로그래밍](#절차-지향-프로그래밍)
    - [예시](#예시)
    - [특징](#특징)
    - [한계](#한계)
  - [객체 지향 프로그래밍](#객체-지향-프로그래밍)
    - [예시](#예시-1)
    - [특징](#특징-1)
  - [둘은 대조 되는 개념이 아니다](#둘은-대조-되는-개념이-아니다)
- [객체와 클래스](#객체와-클래스)
  - [객체](#객체)
  - [클래스](#클래스)
    - [예시](#예시-2)
    - [클래스와 인스턴스](#클래스와-인스턴스)
- [클래스와 인스턴스](#클래스와-인스턴스-1)
- [클래스 구성요소](#클래스-구성요소)
- [메서드](#메서드)
  - [생성자 메서드](#생성자-메서드)
  - [인스턴스 메서드](#인스턴스-메서드)
  - [클래스 메서드](#클래스-메서드)
  - [스태틱 메서드](#스태틱-메서드)
- [데코레이터란 무엇인가?](#데코레이터란-무엇인가)
  - [내용](#내용)
  - [예시](#예시-3)
  - [주요 포인트](#주요-포인트)
  - [추가 학습](#추가-학습)

---

## 절차 지향과 객체 지향

### 절차 지향 프로그래밍

- 함수와 로직 중심 작성
- 데이터를 순차적으로 처리
- 순서대로 작업을 지시하는 방식

#### 예시
```python
name = 'claire'
age = 25

def introduce(name, age):
    print(f'안녕하세요, {age}살 {name}입니다.')
        
introduce(name, age)
```


#### 특징

- 순차적인 명령어 실행
- 데이터와 함수(절차)의 분리
- 함수 호출의 흐름이 중요

#### 한계

1. 복잡성 증가
2. 유지보수 문제 (코드 수정 시 영향 범위 파악이 어려움)

### 객체 지향 프로그래밍

- 클래스는 "설계도", 인스턴스는 "실제 물건"
- 인스턴스는 서로 독립

#### 예시
```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def introduce(self):
        print(f"안녕하세요, {self.name}입니다. 나이는 {self.age}살입니다.")

alice = Person("Alice", 25)
alice.introduce()
```


#### 특징

- 데이터와, 그 데이터를 처리하는 함수(메서드)를 하나의 단위(객체)로 묶음
- 객체가 스스로 기능을 수행하는 능동적 존재가 됨
- 코드 구조화와 재사용성 ↑, 실제 세계 모델링에 적합

### 둘은 대조 되는 개념이 아니다

- 객체 지향은 절차 지향을 기반으로, 객체 개념을 도입해 보완

---

## 객체와 클래스

### 객체

- 실제 존재하는 사물을 추상화한 것
- 속성과 동작(메서드)을 가짐

### 클래스

- 객체를 만들기 위한 설계도
- 데이터와 기능을 함께 묶는 방법을 제공
- 클래스 이름은 파스칼 케이스로 작성

#### 예시
```python
class Singer:
    pass

iu = Singer()
print(type(iu))  # <class '__main__.Singer'>
```

- 아이유는 객체다 (O)
- 아이유는 인스턴스다 (X)
- 아이유는 가수의 인스턴스다 (O)

#### 클래스와 인스턴스

- 변수 `name`의 타입은 str 클래스이다.
- 변수 `name`은 str 클래스의 인스턴스이다.

name = "Alice"
print(type(name))  # <class 'str'>

- "Alice"는 str 클래스의 인스턴스

---

## 클래스와 인스턴스

- 클래스를 정의한다는 것은 공통된 특성과 기능의 틀을 만드는 것
- 실제 객체들은 이 틀에서 생성된 인스턴스

p1 = Person("Alice", 25)
p1.introduce()
p2 = Person("Bella", 30)
p2.introduce()

- 각각의 객체는 독립적으로 속성과 메서드를 가짐

---

## 클래스 구성요소

- 생성자 메서드 (__init__)
    - 인스턴스 생성 시 자동 호출
    - 인스턴스 변수 초기화 담당

- 인스턴스 변수(속성)
    - 각 인스턴스별 고유 속성
    - self.변수명 형태

- 클래스 변수(속성)
    - 모든 인스턴스가 공유하는 속성
    - 클래스 내부에서 직접 정의

- 클래스 변수와 동일한 이름으로 인스턴스 변수 생성 시, 인스턴스 변수가 우선

---
<br>

## 메서드

- 클래스 내부에 정의된 함수
- 객체의 동작을 정의

### 생성자 메서드

- 인스턴스 객체가 생성될 때 자동 호출
- 변수 필요 없으면 __init__(self)만 하고 pass 해도 됨

### 인스턴스 메서드

- 인스턴스의 상태를 조작하거나 동작 수행
- 반드시 첫번째 인자로 인스턴스 자신(self) 받음

- 실제론 str.upper('hello')처럼 동작 (self에 hello가 자동 전달)

### 클래스 메서드

- 클래스 변수를 조작하거나 클래스 레벨의 동작을 수행
- `@classmethod` 데코레이터 사용
- 첫번째 인자로 cls를 받음 (반드시 cls 쓸 필요는 없지만 관례적으로 사용)

### 스태틱 메서드

- 클래스, 인스턴스와 상관없이 독립적으로 동작하는 메서드
- @staticmethod 데코레이터 사용
- 자동으로 전달받는 인자가 없음

---

<br><br>

## 데코레이터란 무엇인가?

### 내용
 
- 함수(또는 클래스)에 새로운 기능을 "간편하게" 추가할 수 있는 문법적 도구  
- 함수를 인자로 받아서, 새로운 함수(또는 클래스)를 반환하는 함수
- @ 기호로 사용

### 예시

```python
def my_decorator(func):
    def wrapper():
        print("함수 실행 전!")
        func()
        print("함수 실행 후!")
    return wrapper

@my_decorator
def hello():
    print("안녕, SSAFY!")

hello()
# 출력
# 함수 실행 전!
# 안녕, SSAFY!
# 함수 실행 후!
```


### 주요 포인트

- 데코레이터는 원본 함수의 동작을 감싸서(before/after 등) 부가기능을 넣을 때 자주 사용  
- 대표적으로 로그 출력, 실행 시간 측정, 권한 체크 등에 활용됨
- 여러 데코레이터를 겹쳐서 사용할 수도 있음

### 추가 학습

- 파라미터가 있는 함수/메서드 데코레이팅
- functools.wraps를 사용해 원본 함수의 정보를 유지하는 법
---
