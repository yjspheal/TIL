- [재귀 함수 (Recursive Function)](#재귀-함수-recursive-function)
  - [개념](#개념)
  - [필수 요소](#필수-요소)
  - [예시: 팩토리얼](#예시-팩토리얼)
- [메모이제이션 (Memoization)](#메모이제이션-memoization)
  - [개념](#개념-1)
  - [목적](#목적)
  - [동작 방식](#동작-방식)
  - [예시: 피보나치 수열](#예시-피보나치-수열)

---

## 재귀 함수 (Recursive Function)

### 개념
- 함수 내부에서 자기 자신을 다시 호출하는 함수.
- 복잡한 문제를 더 작고 동일한 구조의 문제들로 나누어 해결할 때 유용함.

### 필수 요소
1.  **종료 조건 (Base Case)**: 재귀 호출이 멈추는 조건. 이것이 없으면 무한 루프에 빠져 스택 오버플로우가 발생한다.
2.  **재귀 호출 (Recursive Step)**: 자기 자신을 호출하는 부분. 호출할 때마다 문제를 조금 더 작은 단위로 만들어 종료 조건에 수렴해야 한다.

### 예시: 팩토리얼
```python
def factorial(n):
    # 1. 종료 조건 (Base Case)
    if n <= 1:
        return 1
    # 2. 재귀 호출 (Recursive Step)
    else:
        return n * factorial(n - 1)

# factorial(4) = 4 * factorial(3)
#              = 4 * 3 * factorial(2)
#              = 4 * 3 * 2 * factorial(1)
#              = 4 * 3 * 2 * 1 = 24
```

---

## 메모이제이션 (Memoization)

### 개념
- 컴퓨터 프로그램이 동일한 계산을 반복해야 할 때, 이전에 계산한 값을 메모리에 저장해두고, 동일한 입력이 다시 들어오면 저장된 값을 그대로 반환하는 프로그래밍 기법.
- 동적 계획법(Dynamic Programming)의 핵심 기술 중 하나.

### 목적
- **중복 계산 방지**: 이미 계산된 결과를 재활용하여 함수의 실행 속도를 크게 향상시킨다.

### 동작 방식
- 보통 딕셔너리나 배열(캐시, `cache` 또는 `memo`)을 만들어 계산 결과를 저장한다.
- 함수가 호출되면, 먼저 캐시에 원하는 입력값에 대한 결과가 저장되어 있는지 확인한다.
- 결과가 있으면 바로 반환하고, 없으면 계산을 수행한 뒤 그 결과를 캐시에 저장하고 반환한다.

### 예시: 피보나치 수열
메모이제이션이 없을 경우, `fib(5)`는 `fib(3)`을 두 번, `fib(2)`를 세 번 호출하는 등 엄청난 중복이 발생한다.

```python
# 메모이제이션을 위한 캐시 (딕셔너리 사용)
memo = {0: 0, 1: 1}

def fib_memo(n):
    # 캐시에 값이 이미 저장되어 있으면 바로 반환
    if n in memo:
        return memo[n]
    
    # 캐시에 없으면 계산하고, 그 결과를 캐시에 저장
    memo[n] = fib_memo(n - 1) + fib_memo(n - 2)
    return memo[n]

print(f"피보나치(10) with Memoization: {fib_memo(10)}")
```
