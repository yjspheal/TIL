document: |
  # SQL 실습 Day 2 — WHERE / ORDER BY / LIMIT로 조회 감각 만들기

  ## 목차
- [SQL 실습 Day 2 — WHERE / ORDER BY / LIMIT로 조회 감각 만들기](#sql-실습-day-2--where--order-by--limit로-조회-감각-만들기)
  - [목차](#목차)
  - [Day 2 목표](#day-2-목표)
  - [사용 테이블](#사용-테이블)
  - [1. WHERE 조건 조회](#1-where-조건-조회)
    - [목표](#목표)
    - [처음 시도 (실수)](#처음-시도-실수)
    - [문제점](#문제점)
    - [수정 후](#수정-후)
  - [2. 문자열 따옴표에 대한 정리](#2-문자열-따옴표에-대한-정리)
  - [3. ORDER BY 정렬](#3-order-by-정렬)
    - [목표](#목표-1)
    - [처음 시도 (실수)](#처음-시도-실수-1)
    - [문제점](#문제점-1)
    - [수정 후](#수정-후-1)
  - [4. LIMIT으로 결과 개수 제한](#4-limit으로-결과-개수-제한)
    - [목표](#목표-2)
  - [5. Day 2 핵심 패턴 정리](#5-day-2-핵심-패턴-정리)
  - [6. DBA 관점에서 느낀 점](#6-dba-관점에서-느낀-점)
  - [7. Day 2 회고](#7-day-2-회고)
  - [다음 계획 (Day 3)](#다음-계획-day-3)

  ---

  ## Day 2 목표
  - 조건으로 데이터 필터링하기 (`WHERE`)
  - 시간 기준으로 정렬하기 (`ORDER BY`)
  - 필요한 개수만 조회하기 (`LIMIT`)

  어제 만든 `event_logs` 테이블을 그대로 사용해  
  **실무에서 가장 많이 쓰는 조회 패턴**을 손으로 익히는 것이 목표였다.

  ---

  ## 사용 테이블

  ~~~~sql
  event_logs (
    user_id INT,
    event_type VARCHAR(50),
    event_time DATETIME,
    device TEXT
  )
  ~~~~

  ---

  ## 1. WHERE 조건 조회

  ### 목표
  `mobile` 기기에서 발생한 이벤트만 조회

  ### 처음 시도 (실수)

  ~~~~sql
  SELECT *
  FROM event_logs
  WHERE device IS "mobile";
  ~~~~

  ### 문제점
  - `IS`는 **NULL 비교 전용**
  - 문자열 비교에는 사용할 수 없음
  - 문자열을 큰따옴표(`" "`)로 감싼 것도 문제

  ### 수정 후

  ~~~~sql
  SELECT *
  FROM event_logs
  WHERE device = 'mobile';
  ~~~~

  ---

  ## 2. 문자열 따옴표에 대한 정리

  이 과정에서 헷갈렸던 부분은 **작은따옴표 vs 큰따옴표**였다.

  ~~~~
  '문자열'  → 값
  "식별자"  → 컬럼/테이블 이름 (SQL 표준)
  ~~~~

  MariaDB/MySQL 환경에서는  
  문자열은 **작은따옴표만 사용하는 것이 안전**하다.

  ---

  ## 3. ORDER BY 정렬

  ### 목표
  mobile 이벤트를 **최신순**으로 정렬

  ### 처음 시도 (실수)

  ~~~~sql
  ORDER BY event_time descending;
  ~~~~

  ### 문제점
  - SQL에서는 `descending` 같은 자연어를 사용하지 않음
  - 정렬 키워드는 **`ASC` / `DESC`만 허용**

  ### 수정 후

  ~~~~sql
  SELECT *
  FROM event_logs
  WHERE device = 'mobile'
  ORDER BY event_time DESC;
  ~~~~

  ---

  ## 4. LIMIT으로 결과 개수 제한

  ### 목표
  mobile 이벤트 중 **가장 최근 1건만 조회**

  ~~~~sql
  SELECT *
  FROM event_logs
  WHERE device = 'mobile'
  ORDER BY event_time DESC
  LIMIT 1;
  ~~~~

  결과적으로 최신 이벤트 1건만 정상적으로 조회되었다.

  ---

  ## 5. Day 2 핵심 패턴 정리

  Day 2에서 반복적으로 사용한 조회 구조는 다음과 같다.

  ~~~~sql
  SELECT *
  FROM 테이블
  WHERE 조건
  ORDER BY 기준 DESC
  LIMIT N;
  ~~~~

  이 패턴은 로그 조회, 최근 이벤트 확인 등  
  **실무에서 가장 많이 사용되는 형태**다.

  ---

  ## 6. DBA 관점에서 느낀 점

  - `WHERE` → 데이터를 먼저 줄이는 단계
  - `ORDER BY` → 비용이 큰 작업 (정렬)
  - `LIMIT` → 결과 반환 제한 (정렬 비용을 줄여주지는 않음)

  즉,

  > WHERE → ORDER BY → LIMIT  
  > 이 순서를 의식하는 것 자체가 DBA 관점의 SQL 사고방식

  이라는 걸 체감했다.

  ---

  ## 7. Day 2 회고

  - 문법을 아는 것보다 **손으로 쳐보는 것이 훨씬 중요**
  - 실수한 지점들이 오히려 기억에 오래 남음
  - 조회 쿼리의 기본 골격이 자연스럽게 연결되기 시작함

  ---

  ## 다음 계획 (Day 3)

  - 조건 여러 개 사용 (`AND`, `OR`)
  - 날짜 비교 (`>=`, `BETWEEN`)
  - 실무에서 자주 쓰는 조건 조합 연습
