document: |
  # SQL 실습 Day 5 — GROUP BY & 집계(COUNT)로 “요약 테이블” 만들기

  ## 목차
- [SQL 실습 Day 5 — GROUP BY \& 집계(COUNT)로 “요약 테이블” 만들기](#sql-실습-day-5--group-by--집계count로-요약-테이블-만들기)
  - [목차](#목차)
  - [Day 5 목표](#day-5-목표)
  - [실습 데이터 준비](#실습-데이터-준비)
  - [0. 오늘의 실수 포인트](#0-오늘의-실수-포인트)
  - [1. device별 이벤트 개수](#1-device별-이벤트-개수)
  - [2. 집계 결과 정렬하기](#2-집계-결과-정렬하기)
  - [3. event\_type별 이벤트 개수](#3-event_type별-이벤트-개수)
  - [4. device × event\_type 조합별 개수](#4-device--event_type-조합별-개수)
  - [5. 조건(시간) + 집계](#5-조건시간--집계)
    - [5-1) (추가로 해본 버전) 조건 + 2축 집계](#5-1-추가로-해본-버전-조건--2축-집계)
    - [5-2) (요구 미션) 조건 + device별 집계](#5-2-요구-미션-조건--device별-집계)
  - [오늘 배운 규칙 정리](#오늘-배운-규칙-정리)
  - [Day 5 회고](#day-5-회고)
  - [다음 계획 (Day 6)](#다음-계획-day-6)

  ---

  ## Day 5 목표
  - `COUNT(*)`로 개수 세기
  - `GROUP BY`로 범주별 요약 만들기
  - `ORDER BY`로 집계 결과 정렬하기
  - `WHERE`를 결합해서 “조건 + 집계” 패턴 만들기
  - 2축 집계(`GROUP BY a, b`)로 조합별 분포 보기

  ---

  ## 실습 데이터 준비
  오늘은 집계를 재미있게 만들기 위해 `event_logs`에 20줄 샘플 데이터를 넣었다.

  ~~~~sql
  USE sql_practice;

  TRUNCATE TABLE event_logs;

  INSERT INTO event_logs (user_id, event_type, event_time, device) VALUES
  (1, 'view',     '2026-01-14 09:50:12', 'mobile'),
  (2, 'click',    '2026-01-14 09:51:33', 'pc'),
  (3, 'purchase', '2026-01-14 09:52:05', 'tablet'),
  (4, 'view',     '2026-01-14 09:53:47', 'pc'),
  (5, 'click',    '2026-01-14 09:54:21', 'mobile'),

  (1, 'purchase', '2026-01-14 09:55:08', 'pc'),
  (2, 'view',     '2026-01-14 09:56:49', 'tablet'),
  (3, 'click',    '2026-01-14 09:57:14', 'mobile'),
  (4, 'purchase', '2026-01-14 09:58:30', 'tablet'),
  (5, 'view',     '2026-01-14 09:59:55', 'mobile'),

  (2, 'click',    '2026-01-14 10:00:11', 'pc'),
  (3, 'view',     '2026-01-14 10:01:26', 'tablet'),
  (1, 'click',    '2026-01-14 10:02:44', 'tablet'),
  (4, 'view',     '2026-01-14 10:03:09', 'mobile'),
  (5, 'purchase', '2026-01-14 10:04:37', 'pc'),

  (1, 'view',     '2026-01-14 10:05:58', 'tablet'),
  (2, 'purchase', '2026-01-14 10:06:22', 'mobile'),
  (3, 'click',    '2026-01-14 10:07:41', 'pc'),
  (4, 'purchase', '2026-01-14 10:08:16', 'mobile'),
  (5, 'click',    '2026-01-14 10:09:45', 'tablet');
  ~~~~

  데이터가 20줄인지 확인:

  ~~~~sql
  SELECT COUNT(*) AS cnt FROM event_logs;
  ~~~~

  ---

  ## 0. 오늘의 실수 포인트
  처음에는 `GROUP BY` 쿼리를 아래처럼 “두 문장”으로 나눠서 작성했다.

  ~~~~sql
  SELECT device FROM event_logs
  GROUP BY device;
  COUNT(*);
  ~~~~

  문제는 `COUNT(*)`는 **따로 존재할 수 없고**, 그룹핑 결과를 만들 때 **SELECT 안에 함께** 들어가야 한다는 점.

  ---

  ## 1. device별 이벤트 개수
  ~~~~sql
  SELECT device, COUNT(*) AS cnt
  FROM event_logs
  GROUP BY device;
  ~~~~

  ---

  ## 2. 집계 결과 정렬하기
  처음엔 `ORDER BY desc`처럼 정렬 기준이 빠진 실수를 했고,
  정렬은 항상 `ORDER BY <기준> <방향>` 형태라는 걸 확인했다.

  ✅ 올바른 형태:

  ~~~~sql
  SELECT device, COUNT(*) AS cnt
  FROM event_logs
  GROUP BY device
  ORDER BY cnt DESC;
  ~~~~

  ---

  ## 3. event_type별 이벤트 개수
  ~~~~sql
  SELECT event_type, COUNT(*) AS cnt
  FROM event_logs
  GROUP BY event_type
  ORDER BY cnt DESC;
  ~~~~

  (실행 결과 예시: `click 7`, `view 7`, `purchase 6`)

  ---

  ## 4. device × event_type 조합별 개수
  2축 집계(조합별 분포)를 보기 위해 `GROUP BY device, event_type`를 사용했다.

  ~~~~sql
  SELECT device, event_type, COUNT(*) AS cnt
  FROM event_logs
  GROUP BY device, event_type
  ORDER BY device, cnt DESC;
  ~~~~

  결과는 `mobile/pc/tablet × view/click/purchase` 조합으로 여러 행이 나타난다(이번 데이터에서는 9행).

  ---

  ## 5. 조건(시간) + 집계
  “조건으로 먼저 필터링한 뒤 집계”하는 패턴을 연습했다.

  ### 5-1) (추가로 해본 버전) 조건 + 2축 집계
  ~~~~sql
  SELECT device, event_type, COUNT(*) AS cnt
  FROM event_logs
  WHERE event_time >= '2026-01-14 10:00:00'
  GROUP BY device, event_type
  ORDER BY device, cnt DESC;
  ~~~~

  ### 5-2) (요구 미션) 조건 + device별 집계
  ~~~~sql
  SELECT device, COUNT(*) AS cnt
  FROM event_logs
  WHERE event_time >= '2026-01-14 10:00:00'
  GROUP BY device
  ORDER BY cnt DESC;
  ~~~~

  (실행 결과 예시: `tablet 4`, `pc 3`, `mobile 3`)

  ---

  ## 오늘 배운 규칙 정리
  - `GROUP BY`를 쓰면 `SELECT`에는  
    **(1) GROUP BY에 있는 컬럼** 또는 **(2) 집계 함수(COUNT/SUM/AVG...)** 만 올 수 있다.
  - `WHERE → GROUP BY → ORDER BY` 순서를 지킨다.
  - `GROUP BY a, b`는 **a×b 조합별**로 집계한다.
  - `ORDER BY`는 반드시 “기준”이 필요하다: `ORDER BY cnt DESC`

  ---

  ## Day 5 회고
  - “조회”가 아니라 “요약/집계”로 넘어가면서 SQL이 갑자기 실무에 가까워졌다.
  - `GROUP BY`는 문법 자체보다도 **규칙(SELECT에 올 수 있는 컬럼 제한)** 을 이해하는 게 핵심이었다.
  - `조건 + 집계` 조합까지 연결되니, 리포팅 쿼리 형태가 자연스럽게 보이기 시작했다.

  ---

  ## 다음 계획 (Day 6)
  - `HAVING`으로 “집계 결과에 조건 걸기” (예: cnt >= 3)
  - `DISTINCT`와 `COUNT(DISTINCT ...)`
  - (선택) `CASE WHEN`으로 피벗 느낌의 집계 만들기
